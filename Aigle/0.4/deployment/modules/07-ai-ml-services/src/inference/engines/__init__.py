# src/inference/engines/__init__.py
"""
DEPRECATED v2 engines package — only kept to back the legacy `/inference/tts`
endpoint via `VibeVoiceEngine`. New code should use `inference/adapters/` and
`inference/handlers/` instead.
"""
