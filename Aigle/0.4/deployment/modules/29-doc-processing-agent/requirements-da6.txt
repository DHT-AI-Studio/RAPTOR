# DA-6 (EmbeddingSearchTool) runtime dependencies.
# Merge into the module-wide requirements.txt when DA-1 scaffolds the service.
httpx>=0.27
qdrant-client>=1.10
smolagents>=1.20

# Test-only (fixture generation + PDF read for the integration test):
#   pytest, reportlab, pymupdf
