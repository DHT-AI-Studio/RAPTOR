# ==========================================
# Agent Protocol (Module 21)
# ==========================================

# ── Shared infrastructure ─────────────────────────────────────
TIMEZONE=Asia/Taipei
REDIS_HOST=raptor-redis-standalone
REDIS_PORT=6379
REDIS_PASSWORD=<your_redis_password>
REDIS_DB=0
AGENT_TTL=300

# ── Downstream services ───────────────────────────────────────
# Personal DB Service (module 25) — per-user isolated search (was module
# 17/20 hybrid-search/graph-service; those two kept below for rollback)
PERSONAL_DB_SERVICE_URL=http://raptor-personal-db-service:8000
# HYBRID_SEARCH_URL=http://raptor-hybridsearch-api:8000
# GRAPH_SERVICE_URL=http://raptor-graph-service:8843
INFERENCE_URL=http://raptor-ai-lifecycle-api:8010
INFERENCE_THINK=false                   # graphrag-agent / tkg-agent's _summarise() (pipeline.py's _llm_answer() uses LLM_THINK below instead)
ASSET_MANAGEMENT_URL=http://raptor-asset-management:8000

# API 版本（與其他 module 共用，見 root .env.example 的 API_VERSION）。
# 只有在「單獨部署 module 21、沒有 root 的統一 .env」時才會用到這個值；
# 若有 root .env，build.py 會優先用 root 的（見 build.py _env_file_args()）。
API_VERSION=0.4
MEMORY_SERVICE_URL=http://raptor-api-gateway:8012/api/${API_VERSION}
MEM_COMPACT_THRESHOLD=128000

# ── HuggingFace ───────────────────────────────────────────────
HF_TOKEN=<your_huggingface_token>
HF_CACHE_PATH=/path/to/huggingface/cache

# ── Module 21 settings ────────────────────────────────────────
AGENT_ID=raptor-agent
AGENT_NAME=Raptor Intelligence Agent
AGENT_ENDPOINT=http://raptor-agent-protocol:8030
OLLAMA_HOST=http://<your_ollama_host_ip>:11434
OLLAMA_MODEL=qwen2.5:7b
LLM_NUM_CTX=32768
LLM_THINK=false                         # pipeline.py's _llm_answer() -- shared with module 15 via the same root var
LLM_ANSWER_TEMPERATURE=0.3              # RAG answer synthesis (_llm_answer) -- separate from module 15's chat TEMPERATURE (0.7) on purpose

# Module 18 Query Orchestrator
QUERY_ORCHESTRATOR_URL=http://raptor-query-orchestrator:8000

# A2A agent URLs (docker internal network)
VECTOR_AGENT_URL=http://raptor-vector-agent:52001
KEYWORD_AGENT_URL=http://raptor-keyword-agent:52002
GRAPHRAG_AGENT_URL=http://raptor-graphrag-agent:52003
TKG_AGENT_URL=http://raptor-tkg-agent:52004
RERANKER_AGENT_URL=http://raptor-reranker-agent:52005
