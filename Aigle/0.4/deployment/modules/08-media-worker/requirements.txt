# Media Processing Worker - Combined Dependencies
# torch / torchaudio / torchvision   → installed via Dockerfile (cu126 index)
# paddlepaddle-gpu                   → installed via Dockerfile (cu123 index)
# paddleocr                          → installed via Dockerfile (separate step)

# Kafka / async
aiokafka==0.12.0
aiofiles==24.1.0
aiohttp==3.12.15

# Audio: ASR + Diarization (audio_recognizer, audio_diarization)
whisperx==3.3.1
librosa==0.11.0
scipy==1.15.3

# Audio: Classification (audio_classifier)
panns_inference==0.1.1

# Vision / VLM (image_processing, video_frame_description, document_analysis)
transformers==4.51.3
einops
accelerate
timm
decord==0.6.0

# Video / Image processing
ffmpeg_python==0.2.0
opencv-python

# Document parsing (document_analysis)
PyMuPDF==1.26.4
docling==2.54.0
pandas==2.3.3

# Graph (document_graph)
neo4j>=5.0,<6.0

# HuggingFace — pin <1.0 for whisperx/pyannote use_auth_token compatibility
huggingface_hub>=0.21,<1.0.0

# Sync APIs
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.9

# Utilities
numpy==1.26.4
Pillow==11.3.0
psutil==6.1.1
Requests==2.32.5
tenacity==9.1.4
python-dotenv==1.1.1
zhconv==1.4.3
langsmith==0.4.32
redis
platformdirs
cffi
jsonpatch
opencc-python-reimplemented==0.1.7
