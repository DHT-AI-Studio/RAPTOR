# Vision Processing Worker Dependencies
# GPU: InternVL (image_processing, video_frame_description)
#      PaddleOCR (video_ocr_frame)

# Kafka / async
aiokafka==0.12.0
aiofiles==24.1.0
aiohttp==3.12.15

# VLM / InternVL (image_processing + video_frame_description)
# paddlepaddle-gpu is installed separately in Dockerfile (custom index)
transformers==4.51.3
timm
torch==2.7.1
torchvision==0.22.1
decord==0.6.0

# OCR (video_ocr_frame)
paddleocr==3.2.0

# Video processing (video_analysis, video_scene_detection, video_chunking)
ffmpeg_python==0.2.0
opencv-python

# Sync API (video_frame_description_service)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.9

# Utilities
numpy==1.26.4
Pillow==11.3.0
psutil==6.1.1
Requests==2.32.5
python-dotenv==1.1.1
zhconv==1.4.3
langsmith==0.4.32
redis
platformdirs
cffi
jsonpatch

# Open Chinese Convert (for text processing)
opencc-python-reimplemented==0.1.7
