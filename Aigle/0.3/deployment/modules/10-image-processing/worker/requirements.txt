# Vision Processing Worker Dependencies
# GPU: InternVL (image_processing, video_frame_description)
#      PaddleOCR (video_ocr_frame)

# Kafka / async
aiokafka==0.12.0
aiofiles==24.1.0
aiohttp==3.12.15

# VLM / InternVL (image_processing + video_frame_description)
# paddlepaddle-gpu is installed separately in Dockerfile (custom index).
# torch / torchvision are installed in the Dockerfile from the cu128 wheel
# index (https://download.pytorch.org/whl/cu128) so we get sm_120 kernels
# for RTX 50-series (Blackwell). Do NOT add torch/torchvision pins here —
# that would let pip resolve a default PyPI build that lacks sm_120.
transformers==4.51.3
einops
accelerate
timm
decord==0.6.0

# OCR (video_ocr_frame)
paddleocr==3.2.0

# Video processing (video_analysis, video_scene_detection, video_chunking)
ffmpeg_python==0.2.0
opencv-python

# Utilities
numpy==1.26.4
Pillow==11.3.0
psutil==6.1.1
Requests==2.32.5
python-dotenv==1.1.1
zhconv==1.4.3
langsmith==0.4.32
redis
platformdirs
cffi
jsonpatch

# Sync API (image_processing)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.9

# Open Chinese Convert (for text processing)
opencc-python-reimplemented==0.1.7
