# Audio Processing Worker Dependencies
# GPU: whisperx (audio_recognizer, audio_diarization)
#      panns_inference (audio_classifier)

# Kafka / async
aiokafka==0.12.0
aiofiles==24.1.0
aiohttp==3.12.15

# ASR + Diarization (audio_recognizer + audio_diarization)
whisperx==3.3.1
# torch / torchaudio are installed in the Dockerfile from the cu128 wheel
# index (https://download.pytorch.org/whl/cu128) so we get sm_120 kernels
# for RTX 50-series (Blackwell). Do NOT add torch/torchaudio pins here —
# that would let pip resolve a default PyPI build that lacks sm_120.
# huggingface_hub>=1.0 removed use_auth_token; pyannote.audio 3.3.2 needs <1.0
huggingface_hub>=0.21,<1.0.0
# transformers 5.x requires huggingface_hub>=1.0, pin to 4.x
transformers>=4.36,<5.0

# Audio classification (audio_classifier)
panns_inference==0.1.1

# Audio processing (audio_analysis)
librosa==0.11.0
scipy==1.15.3

# Sync API (audio_recognizer_service)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.9

# Utilities
numpy==1.26.4
Requests==2.32.5
python-dotenv==1.1.1
psutil==6.1.1
redis
platformdirs
cffi

# LangSmith tracing
langsmith==0.4.32

# FFmpeg Python wrapper
ffmpeg_python==0.2.0

# Open Chinese Convert (for text processing)
opencc-python-reimplemented==0.1.7
