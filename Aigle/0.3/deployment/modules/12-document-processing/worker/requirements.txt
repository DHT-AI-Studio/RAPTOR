# Document Processing Worker Dependencies
# GPU: InternVL (document_analysis_service) — requires CUDA

# Kafka / async
aiokafka==0.12.0
aiofiles==24.1.0
aiohttp==3.12.15

# Document parsing
PyMuPDF==1.26.4
docling==2.54.0
pandas==2.3.3
zhconv==1.4.3

# VLM / OCR  (document_analysis_service)
# paddlepaddle-gpu is installed separately in Dockerfile (custom index).
# torch / torchvision are installed in the Dockerfile from the cu128 wheel
# index (https://download.pytorch.org/whl/cu128) so we get sm_120 kernels
# for RTX 50-series (Blackwell). Do NOT add torch/torchvision pins here —
# that would let pip resolve a default PyPI build that lacks sm_120.
paddleocr==3.2.0
transformers==4.51.3
einops
timm

# Utilities
numpy==1.26.4
Pillow==11.3.0
psutil==6.1.1
Requests==2.32.5
python-dotenv==1.1.1
redis
platformdirs
cffi
jsonpatch

# Neo4j (document_graph_service)
neo4j>=5.0,<6.0

# Sync API (document_analysis_service)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.9

# Open Chinese Convert (for text processing)
opencc-python-reimplemented==0.1.7