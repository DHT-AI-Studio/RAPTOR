# ==========================================
# Kafka Cluster
# ==========================================
KAFKA_CLUSTER_ID=<your_kafka_cluster_id>

# Host IP for external listener (set to this machine's LAN IP)
HOST_IP=<your_host_ip>
